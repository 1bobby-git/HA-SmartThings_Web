name: Light dispatch workspace
on:
  push:
    branches: [perf/light-dispatch-1.8.28]
    paths: [.github/workflows/light-dispatch-workspace.yml]
permissions:
  contents: read
jobs:
  snapshot:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: 99c41c454060712b74f1cc705afc875ae989e2c3
          persist-credentials: false
      - name: Archive tracked baseline only
        run: git archive --format=tar HEAD | gzip > light-dispatch-baseline.tar.gz
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: light-dispatch-baseline
          path: light-dispatch-baseline.tar.gz
          retention-days: 1
