## 1.8.32

- Advanced 요청의 확정적인 401/인증 리디렉션을 현재 keeper의 로그인 복구 상태에 즉시 전달합니다. 다음 5분 유지 검사까지 기다리지 않으며, 403/429/통신 실패를 로그아웃으로 바꾸거나 실패한 POST를 재전송하지 않습니다.
- 이전 인증 조회가 성공했던 페이지에서 로그인이 필요해지면 30초 유예 후 같은 전용 프로필의 별도 탭으로 정상 SSO 복구를 확인합니다. 보호된 조회가 성공한 경우에만 keeper를 교체하고, 실패·추가 인증 필요·사용자 작업 중에는 원래 로그인 폼을 유지합니다. 재시도 간격은 5분이며 브라우저에 처음 로그인하는 경우의 기존 유예는 유지합니다.
- 같은 URL로 관리형 새로고침을 한 뒤 이전 로그인 검사 결과나 멈춘 검사 lease가 새 검사를 막지 않도록 세대를 분리합니다. 복구가 진행 중이면 페이지 URL만 보고 로그인 정상으로 되돌리지 않습니다. `session_authentication_rejected`와 `session_recovery` 로그로 거부/시도/검증/수동 로그인 필요를 구분합니다.
- **선택적 조명 묶음 전송** `light_command_batch_enabled`를 추가합니다(기본 false). true이면 검증된 같은 전구·컴포넌트의 on/밝기/setColor/색온도 명령들을 기존 Advanced commands 배열의 한 POST로 보내 순차 왕복과 사전 조회를 줄입니다. 서로 다른 기기·명령의 병렬 전송이나 장치 실행 원자성을 보장하는 기능이 아닙니다.
- 묶음 모드도 모든 인수 검증, 각 명령의 접수 결과와 전체 목표 상태의 실제 확인을 유지합니다. 일부 실패·불명확한 접수·시간 초과를 자동으로 순차 재전송하지 않습니다. 별도 hue/saturation 명령과 단독 전원은 기존 경로를 유지합니다. 실제 전구 검증 전에는 기존 동작을 보존하도록 기본 false이며, 앱 구성에서 true로 변경하고 앱을 재시작해야 사용됩니다.
- 합성 800ms POST 조건에서 세 명령은 POST 3회→1회로 접수 대기 1600ms를 줄였습니다. 사용자 전구의 실측 수치나 단독 POST의 약 0.8초 지연 제거를 뜻하지 않습니다. 정상 setColor 계약·명령 이후 확인·Home Monitor·세션 프로필·엔티티 ID·protocol 5를 유지합니다. 서버 세션 폐기나 MFA 요구를 우회하지 않습니다.

## 1.8.31

- 조명 사전 확인에서 등록 정보의 `Main`과 상태 응답의 `main`처럼 역할 문자열의 대소문자만 다른 경우 불필요하게 중복 `on`을 전송하던 경로를 수정합니다. 기기·위치·컴포넌트·기능 ID, 값과 단위는 그대로 정확히 비교합니다.
- 위치 이벤트 시각이 Advanced 속성 변경 시각보다 나중이어도 값이 같은 경우, 기존 400ms 전체 예산 안에서 두 번째 독립 대상 GET까지 일치할 때만 중복 전원/동일 밝기를 생략할 수 있습니다. 오래된 같은 출처 스냅샷은 계속 거부하며 기존 상태·시각을 덮어쓰지 않습니다.
- `dispatch`에 `preflightReason`과 `preflightReads`를 추가해 중복 생략 여부와 구체적인 검증 탈락 사유를 구분합니다. 이전 사용자 로그에는 탈락 사유가 없어 실제 기기의 어떤 조건이 막혔는지까지 확정하지 않습니다.
- 다른 대상·값·단위·역할·시각 충돌, 조회 실패·지연, 중간 상태 변경이나 취소에서는 원래 명령을 유지하거나 기존 취소 경로를 따릅니다. 실제 색상/색온도/밝기 변경 명령과 단독 전원 명령은 생략하지 않습니다. 전체 목표의 명령 이후 확인은 그대로 유지합니다.
- 정상 동작하는 setColor와 Home Monitor, 로그인 유지/복구, 최신 요청 우선 처리, 엔티티 ID 및 protocol 5를 유지합니다. 단일 POST의 네트워크/서버/브라우저 대기 자체를 없애거나 요청값을 실제 상태로 표시하는 변경은 아닙니다. Bridge와 HACS 통합을 1.8.31로 함께 업데이트하고 재시작하세요.

## 1.8.30

- 기존 전용 Chromium 프로필과 세션 복원 설정을 유지하면서 로그인 유지 요청을 보강합니다. 일반 페이지 GET은 최대 3초, 인증된 위치 조회는 별도 최대 9초 예산으로 실행해 앞선 페이지 지연이 실제 인증 확인을 막지 않게 합니다. 계정·기기 제어 없이 기존 동일 출처 GET만 사용합니다.
- HTTP 200만으로 로그인 유지 성공을 판단하지 않고 JSON 위치 목록을 확인합니다. 401/인증 리디렉션은 재인증 필요, 403/429/5xx/네트워크/잘못된 응답은 통신 실패로 구분하며 쿠키나 프로필을 지우지 않습니다. 같은 페이지의 중복 검사와 늦게 도착한 이전 페이지 결과도 차단합니다.
- 정상 유지 간격은 5분이며 일시 실패에는 30초→60초→120초→240초→최대 5분으로 재시도합니다. 브라우저 스크립트가 응답하지 않아도 외부 대기를 제한하고, 이미 진행 중인 같은 검사를 계속 쌓지 않습니다.
- 브라우저 시작의 즉시 재시도를 소진해도 기존 프로필로 1분→2분→4분→최대 5분 간격의 복구 시도를 계속합니다. 종료 요청 후에는 재시도하지 않습니다. 세션 복구 새로고침은 진행 중 기기 제어와 충돌하지 않도록 보류하며, WebSocket 복구가 Samsung 로그인 폼을 덮어쓰지 않게 합니다.
- 기억된 세션 복구에서 /location 화면이 로드됐다는 이유만으로 인증 성공 처리하지 않습니다. 보호된 조회가 성공해야 복구 대기 상태를 해제합니다. 실제 계정 확인/추가 인증이 필요하면 사용자 로그인은 계속 필요합니다.
- Bridge 상태 페이지/health에 sessionTouchCount, sessionTouchConsecutiveFailures, sessionTouchLastOutcome, sessionTouchAgeMs, sessionTouchSuccessAgeMs를 추가하고 session_keepalive 로그에 성공/실패·소요 시간·다음 확인 간격만 표시합니다. 계정 정보·응답 본문·쿠키·토큰을 출력하지 않습니다.
- 1.8.29의 조명 명령/지연 최적화, Home Monitor 제어, 로그인 프로필 위치, 엔티티 ID, protocol 5와 의존성은 유지합니다. 서버의 강제 만료·세션 폐기·추가 인증을 무력화하거나 영구 로그인을 보장하는 변경은 아닙니다. Bridge 앱과 HACS 통합을 1.8.30으로 업데이트하고 재시작하세요.

## 1.8.29

- 연속 조명 조작에서 직전 성공 명령의 실제 대상 상태 GET을 조회 시작 후 1초까지만 한 번 재사용합니다. 같은 기기/위치/keeper, 변경 없는 상태 revision, 실제 값·역할·단위·timestamp 검증이 모두 맞을 때만 중복 전원/동일 밝기 사전 GET을 생략합니다. 영구 캐시나 요청값을 사용하지 않으며 새 명령 이후 전체 목표 상태 확인은 유지합니다.
- 사전 조회가 실패하거나 느리면 다음 5초 동안 선택적 사전 GET만 쉬고 기존 전체 명령을 바로 전송합니다. 명령을 5초 대기시키는 기능이 아닙니다. 취소된 조작은 조회 장애로 계산하지 않으며 세션 변경 시 이 제한도 초기화합니다.
- `command_device`의 `preflightSource`로 `live_read`/`recent_read`/`backoff`를 구분합니다. `advanced_request_timing`은 개별 POST의 브라우저 처리, 응답 헤더까지의 fetch, JSON 본문 처리, Bridge 왕복 부가 시간을 숫자로 기록합니다. URL·원본 ID·인수·쿠키·토큰은 기록하지 않습니다. fetch 시간은 서버 처리만을 의미하지 않습니다.
- 정상 동작한 setColor/색온도 형식, 순차 POST, 최신 요청 우선 처리, 상태 확인 조건, Home Monitor 및 로그인 세션과 엔티티 ID를 유지합니다. 만료/상태 변경/재연결/다른 명령/실패 시 증거를 재사용하지 않습니다. 상태 재확인을 끈 설정에서는 두 최적화 모두 비활성입니다.
- POST 800ms / GET 100ms 합성 검사에서 연속 조작의 확인은 901ms → 801ms입니다. 이는 사용자 전구의 실측이나 단독 전원 제어의 약 0.8초를 없앤 결과가 아닙니다. Bridge와 HACS 통합을 1.8.29로 함께 업데이트하고 재시작하세요.

## 1.8.28

- 조명 속성 변경 전 이미 켜진 전구만 짧은 대상 GET으로 확인합니다. 새 응답과 기존 관측 상태가 일치하고 중간 변경이 없으면 중복 on과, 색상/색온도 변경에 딸린 동일 setLevel만 생략합니다. 색상·색온도 명령과 전원 단독 명령은 생략하지 않습니다.
- 사전 조회는 기존 로그인 keeper에서만 수행하며 브라우저 GET 제한 350ms, 전체 최적화 대기 제한 400ms입니다. 조회 실패·시간 초과·오래된 응답·다른 대상·중간 이벤트에서는 기존 명령 전체를 유지합니다. 추가 탭이나 재전송을 만들지 않습니다.
- 사전 조회 값을 성공이나 HA 상태로 선반영하지 않습니다. 생략한 전원/밝기를 포함한 전체 목표 상태는 기존 이벤트/명령 이후 조회로 확인하며, 마지막 요청의 실제 실패는 유지합니다.
- 발견과 명령 전송에서 기능 정의 캐시를 공유해 첫 조작의 중복 capability GET을 없앴습니다. 상태 재확인을 끈 설정에서는 사전 조회 최적화도 수행하지 않습니다.
- dispatch 로그에 skippedCommands 및 preflightMs를 추가했습니다. setColor, 순차 POST, 최신 요청 우선, Home Monitor, 로그인 세션, 엔티티 ID, protocol 5와 의존성은 유지합니다.
- 800ms POST / 100ms GET을 가정한 합성 회귀에서 이미 켜진 동일 밝기 전구의 색상 변경은 POST 3회→1회, 확인까지 2401ms→901ms였습니다. 사용자 전구 실측 개선 수치가 아니며, 전원 단독 명령의 약 0.8초 접수 지연을 없애는 변경은 아닙니다. Bridge와 HACS 통합을 모두 1.8.28로 업데이트하고 재시작하세요.

## 1.8.27

- 정확히 조회한 기존 조명의 상태는 전체 인벤토리 재요청 대신 변경된 상태만 SSE로 전달합니다. source와 기기 timestamp를 보존하며, 등록 정보/새 속성이 있는 응답과 일반 상태 조회는 기존 인벤토리 경로를 유지합니다.
- HA는 Bridge가 두 번의 일치하는 조회로 확인한 동일 timestamp 조명 값도 순서가 맞는 상태 이벤트로 반영합니다. 이전 sequence, 오래된 timestamp, 검증되지 않은 출처, 보안 상태에 대한 예외는 허용하지 않습니다. 연결 복구와 sequence 누락의 전체 동기화는 유지합니다.
- 조명 묶음의 명령 접수 후 첫 재확인은 즉시 시작하고 초기 확인 간격을 줄입니다. 30초 제한에서 최대 8회인 기존 재확인 예산, 직렬 READ, 완료/대체/시간 초과 취소를 유지합니다. 초기 재확인이 꺼져 있으면 마지막 확인만 유지합니다. Home Monitor의 확인 일정은 변경하지 않습니다.
- 조명 조회에서 불필요한 전체 카메라 인벤토리 순회를 생략합니다. command_device에 receipt 단계와 readMs를 추가해 전송 접수 대기와 실제 상태 조회 소요를 구분합니다.
- 정상 동작한 setColor/ColorMap, 명령 전송 순서, 최신 요청 우선 처리, 로그인, 엔티티 ID 및 Home Monitor 제어는 유지합니다. 요청값을 실제 상태로 표시하거나 접수만으로 성공 처리하지 않습니다.
- Bridge 앱과 HACS 통합을 모두 1.8.27로 업데이트해야 동일 timestamp 변경값의 직접 전달 경로가 완전히 적용됩니다. 합성 지연/회귀 검사와 실제 사용자 전구의 반응 시간은 구분합니다.

## 1.8.26

- 색상 명령 `setColor`의 정상적인 ColorMap 정의가 `schema_invalid`로 제외되는 검증 조건을 수정합니다. 색상·채도의 선택적 최소/최대 제약과 `hex`/`level`/`switch` 선택 속성을 허용하되, 실제 전송은 hue/saturation 두 값만 0~100 범위로 제한합니다. 명시된 더 좁은 범위, 정수 조건, 추가 필수 속성, 미지원 복합 제약은 우회하지 않습니다.
- 정상화된 카탈로그에서 HA가 기존 `setColor` 우선 경로를 선택하도록 회귀 검사를 추가했습니다. 개별 setHue/setSaturation만 동작한다고 가정하지 않습니다. 원격 접수만으로 기기 상태 변경을 성공 처리하지 않습니다.
- `command_device` 로그의 dispatch 단계에 검증된 명령명 목록을 추가해 setColor/분리 전송 여부를 확인할 수 있습니다. 원본 ID, 토큰, 쿠키, 요청 본문은 추가로 출력하지 않습니다.
- 1.8.25의 최신 요청 우선 처리, 세션, 기존 엔티티 ID 및 Home Monitor 동작은 유지합니다. Bridge 앱과 HACS 통합을 모두 업데이트하고 재시작하세요.
- 공개 ColorMap 구조와 합성 장치로 회귀 현상을 재현한 수정입니다. 사용자 dev_300의 원본 capability 정의 및 실제 전구 동작은 아직 직접 검증하지 않았습니다. 제외 수의 합계만으로 다른 명령까지 같은 원인이라 판단하지 않습니다.

## 1.8.25

- 검증된 전구는 Advanced의 `on`/`off`, `setLevel`, `setColorTemperature`, `setColor`(또는 실제 카탈로그의 hue/saturation) 명령을 개별 POST로 순서대로 전송합니다. 모든 입력을 전송 전에 검사하고, 여러 상태의 최종 확인은 함께 수행합니다. 불확실한 POST 자동 재시도나 DOM 우회 재전송은 하지 않습니다.
- 새 Bridge 기능 협상(`lightLatestWinsSupported`)이 확인된 HA 조명에서 연속 요청을 최신 의도로 교체합니다. 첫 조작은 즉시, 연속 슬라이더는 75ms 병합 후 마지막 요청을 보냅니다. 진행 중인 POST는 끝날 때까지 직렬화하고, 취소된 이전 묶음의 나머지 명령/상태 대기는 중단합니다. 대체된 요청은 실패 알림을 쌓지 않지만 최종 요청의 실제 오류는 유지합니다.
- 밝기 조작 직후 색상을 바꿔도 아직 처리 중인 밝기 설정을 잃지 않습니다. 색상↔색온도와 끄기는 서로 충돌하는 이전 설정을 제거합니다. 사용자에게 표시하는 상태는 요청값이 아니라 관측된 기기 값입니다.
- 명령 직후 전구 상태 GET의 값은 달라졌지만 timestamp가 없거나 같아 캐시에 반영되지 않던 경로를 보완합니다. 같은 기기/컴포넌트의 독립된 GET 두 번이 일치하고 그 사이 새 이벤트/새 조명 명령이 없을 때만 실제 응답으로 보정합니다. 기존 timestamp는 재생 방지 기준으로 유지하며, 더 오래된 timestamp·보안 상태·일반 인벤토리에는 예외를 적용하지 않습니다.
- `command_device` 진단에 요청/관측 전구 값과 대체(`superseded`) 단계를 추가합니다. 원본 기기 ID·쿠키·토큰·본문은 출력하지 않습니다.
- 잘 동작하는 Home Monitor의 전송·확인·진행 표시 코드는 변경하지 않습니다. 세션, 엔티티 ID, 기존 호환 경로와 protocol 5를 유지합니다. **Bridge 앱과 HACS 통합을 모두 1.8.25로 업데이트하고 재시작**해야 새 조명 경로가 활성화됩니다.
- 회귀 검사는 합성 장치와 실제 코드 경로로 수행합니다. 사용자 HA 설치 및 안방 무드등의 물리 동작 검증과는 구분합니다.

## 1.8.24

- 조명 묶음에서 이미 켜진 전원·변경하지 않은 밝기/색상 채널에도 새 이벤트를 요구해 확인이 지연되던 조건을 수정합니다. 변경한 속성과 요청한 색 모드의 새 증거를 확인하며, 캐시만으로 완료하거나 명령을 자동 재전송하지 않습니다.
- 개별 밝기·색온도·색상 명령도 검증된 조명에 한해 묶음 제어와 같은 반올림 허용 범위로 확인합니다. 명시적으로 다른 색 모드가 보고되면 스칼라 값만 맞는 상태를 색 모드 변경 성공으로 처리하지 않습니다.
- 상태 재확인이 활성화된 경우 첫 대상 조회를 1,000ms에서 250ms로 앞당깁니다. 조회는 명령별 제한 시간 안에서 직렬 실행하고, 이벤트로 완료되면 취소합니다. 이는 조회 시작 지연이며 실제 기기 반응 시간 보장이 아닙니다.
- HA가 확인된 조명 상태와 해당 sequence를 이미 받았다면 중복 전체 인벤토리 조회를 생략해 다음 전원/슬라이더 명령의 대기를 줄입니다. 누락·불일치·부분 실패에서는 기존 상태 보정과 오류를 유지합니다.
- Home Monitor는 HA의 `alarm_state` 열거형을 사용하고 집·외출·해제 요청 중 진행 상태를 즉시 표시합니다. 실제 확인 전에는 완료 상태를 만들지 않으며, 오류·취소 시 진행 표시를 해제합니다. 경보 발생/진입 대기는 진행 표시보다 우선합니다.
- Bridge 앱과 HACS 통합을 모두 1.8.24로 업데이트하고 재시작하세요. 1.8.23의 로그인 유지, 엔티티 ID, 보안 요청·중복 방지, 시간 순서 보호, protocol 5와 의존성은 유지합니다. 합성/회귀 테스트는 실제 사용자 기기의 설치·작동 검증과 구분합니다.

## 1.8.23

- 로그인 유지 확인 시 `/location`의 리디렉션·권한 오류만으로 로그인 만료로 판단하지 않고, 인증된 위치 조회의 결과를 확인합니다. HTTP 403·서버/네트워크 오류 때문에 정상 세션을 재로그인으로 전환하지 않습니다.
- 삼성 로그인이 새 위치 탭에서 완료되면 해당 탭을 관찰용 탭으로 인계하고 이전 로그인 탭을 닫습니다. 기존 관찰용 탭과 기기 제어용 탭의 분리를 유지합니다.
- 브라우저 시작 중 확인된 인증·연결 상태를 시작 완료 처리에서 `LOGIN_REQUIRED`로 덮어쓰지 않습니다.
- 기존 영구 Chromium 프로필, 5분 주기 세션 유지 요청, 저장된 삼성 세션을 이용한 복구를 유지합니다. 삼성에서 재인증을 요구하면 웹 UI에서 로그인·QR 승인이 필요하며 무기한 로그인 유지를 보장하지 않습니다.

## 1.8.22

- 숫자 센서가 NaN, 무한대 또는 안전하게 표현할 수 없는 큰 숫자를 Home Assistant 상태로 게시하지 않도록 수정합니다. 정상적인 0·음수·소수는 유지합니다.
- 수신 신호 지표의 잘못된 숫자는 제외하고 함께 수신한 정상 RSSI/LQI는 계속 표시하여 숫자 변환 오류를 방지합니다.
- 모델 회귀 검사 102개가 통과했습니다. 실제 기기 입력 및 사용자 Home Assistant 설치 검증은 이 릴리스 노트에서 주장하지 않습니다.

## 1.8.21

- 같은 밝기를 다시 요청할 때 저장된 값만으로 `already_confirmed` 처리하던 경로를 수정합니다. 현재 카탈로그에서 검증된 `setLevel`은 정확한 기기·위치·속성의 최신 조회와 현재 상태가 모두 일치할 때만 명령을 생략합니다. 조회가 실패하거나 실제 밝기가 다르면 검증된 명령을 한 번 전달하고 기존 상태 확인을 유지합니다.
- 개별 명령과 묶음 조명의 상태 확인은 전체 인벤토리 대신 해당 기기의 상태만 읽습니다. 반환값은 원본 캐시와 분리하고, 오프라인·다른 위치·카메라 메타데이터 필터와 상태 시각·순서 보호를 유지합니다. 보안 상태 확인이나 명령 제한 시간을 변경하지 않습니다.
- Bridge 앱과 HACS 통합을 모두 1.8.21로 맞춰 재시작하세요. 기존 조명 묶음 전송·팬 전원 수정·엔티티 ID·로그인·방/재실·Home Monitor·Scene/TTS·짧은 README·의존성 및 protocol 5를 유지합니다. 실제 사용자 HA 설치와 기기 제어 결과는 원격 확인하지 않았습니다.

## 1.8.20

- 조명 색상은 확인된 `setColor` 명령이 있으면 hue/saturation 두 값을 함께 전송합니다. 해당 명령의 평면 숫자 객체 규격만 지원하고, 타입·범위·필수 속성과 위험/민감 명령 검증은 유지합니다. 임의 객체 스키마를 허용하지 않습니다.
- 전원·밝기·색상을 단계마다 대기시키지 않고 같은 조명의 검증된 명령을 한 번의 Advanced 요청으로 전달한 뒤 실제 결과 상태를 함께 확인합니다. 중간 hue 확인 때문에 saturation이나 밝기 전송이 막히는 구조를 제거합니다. 새 기능은 Bridge가 명시적으로 알릴 때만 사용하며, 기존 Bridge/제어 경로와 엔티티 ID를 보존합니다.
- 기기의 실제 조명 보고 해상도를 고려해 밝기·채도 0.5퍼센트포인트, 원형 hue 0.5퍼센트포인트, 색온도 1 mired 이내만 같은 결과로 확인합니다. HA에는 요청값이 아니라 실제 수신값을 표시하며 일반 숫자·보안 명령의 비교는 변경하지 않습니다.
- 전원과 조명 숫자 제어는 현재 카탈로그·버전·대상이 정확히 확인될 때 Advanced 경로를 우선합니다. 전원 캐시가 요청과 같아도 실제 기기 상태를 다시 대조하여 오래된 캐시만으로 명령을 생략하지 않습니다. 불확실한 POST를 다른 브라우저 경로로 반복하지 않습니다.
- 공기청정기/팬의 전원 표시·상태·실행 대상을 동일하게 선택합니다. action/detail 중복 전원 항목 때문에 표시된 버튼이 실행되지 않던 오류를 수정하고, 다른 컴포넌트와 모호한 대상은 선택하지 않습니다.
- 별칭 기기·단계·상태 일치 여부만 기록하는 제어 진단을 보강합니다. 확인 실패를 성공으로 바꾸거나 대기 시간을 늘리지 않습니다. Bridge 앱과 HACS 통합 모두 1.8.20으로 업데이트 후 재시작하세요. 로그인·방/재실·Home Monitor·Scene/TTS·짧은 README·의존성과 protocol 5를 유지합니다. 실제 사용자 기기 정상화는 원격 검증하지 않았습니다.

## 1.8.19

- 조명 명령 후 상태 확인을 한 번의 초기 조회에 의존하지 않도록 수정합니다. 기본 30초 확인 시간 안에서 해당 기기의 읽기 전용 상태를 제한적으로 다시 조회하며, 성공·실패 후 조회를 중단합니다. 명령을 반복 전송하거나 제한 시간을 늘려 숨기지 않습니다.
- 색상·색온도 값을 다시 설정해 새 이벤트가 발생하지 않아도, 명령 접수 이후 시작된 해당 기기/위치/컴포넌트/기능의 실제 GET 응답과 현재 상태가 정확히 일치하면 상태 확인으로 처리합니다. 다른 기기의 이벤트, 누락된 상태, 오래된 조회, 단순 접수 응답은 성공 근거로 사용하지 않습니다. SSE 순서나 기기 타임스탬프를 만들어내지 않습니다.
- 조명 작업 종료 후 HA가 로컬 Bridge의 실제 상태를 한 번 동기화하여 실시간 스트림 지연과 일부 단계 실패 뒤의 실제 상태를 반영합니다. 실패는 그대로 알리고 입력값으로 상태를 덮어쓰지 않습니다.
- 반복 조작으로 대기 요청이 쌓이면 실행 전 10초 대기 한도에서 만료시켜 나중에 오래된 조작이 실행되지 않도록 합니다. 이미 전송 중인 명령은 중단하지 않습니다. 오류에는 전원·밝기·색상·색온도 중 실패한 단계를 표시합니다.
- Bridge 앱과 HACS 통합을 모두 1.8.19로 업데이트 후 재시작하세요. 기존 엔티티 ID·로그인·방/재실·Home Monitor·Scene·TTS·짧은 README·의존성과 protocol 5는 유지합니다. 실제 사용자 전구와 HA 연결 상태는 원격 검증하지 않았습니다.

## 1.8.18

- Advanced 명령 스키마의 설명용 `title`·`description` 문자열을 검증 제약과 분리합니다. `PositiveInteger`·`PositiveNumber` 제목이 있는 밝기 `setLevel`과 색상 `setHue`·`setSaturation`이 `schema_invalid`로 빠지던 경로를 수정합니다. 설명 원문은 공개 카탈로그에 전달하지 않습니다.
- 실제 숫자 타입·범위·필수 인수 검사는 유지하고, `optional: true` 속성은 원본/정규화 카탈로그 모두에서 일관되게 해석합니다. 처리하지 못하는 객체·배열·참조·추가 제약과 위험/민감 명령을 무조건 허용하지 않습니다.
- 원본 형태 스키마 → 캐시 파서 → 카탈로그 → HA 파서 → 조명 엔티티를 공유 테스트 데이터로 검증합니다. 재시작 후에도 on/off만 남는 카탈로그 누락과 선택적 rate 인수, 기존 엔티티의 밝기·색상·색온도 복원을 검사합니다. 지원하지 않는 다른 명령의 `schema_invalid` 수는 남을 수 있습니다.
- Bridge 앱과 HACS 통합을 모두 1.8.18로 업데이트하고 재시작하세요. 기존 로그인·기기 ID·전원 제어·방/재실·Home Monitor·Scene·TTS·짧은 README·의존성과 protocol 5는 유지합니다. 실제 사용자 전구 조작은 수행하지 않았습니다.

## 1.8.17

- 조명의 밝기·색온도·색상 제어를 보완합니다. Web 상세 슬라이더가 아직 없더라도 실제 수신 상태와 같은 컴포넌트·기능의 Advanced 명령을 대조해 `setLevel`, `setColorTemperature`, `setHue`, `setSaturation`을 연결합니다. 실제 지원되는 조명에 HA 색상 선택(HS)과 색온도를 함께 제공합니다.
- 조명 생성 시 고정되던 지원 기능을 현재 인벤토리 기준으로 갱신합니다. 늦게 도착한 명령·슬라이더와 밝기·색상만 바뀌는 실시간 이벤트도 기존 조명 엔티티에 반영하며 다른 컴포넌트의 값을 가져오지 않습니다.
- SmartThings의 선택적 `rate` 인수(`optional: true`)를 인식합니다. 밝기 0~255와 native 0~100, HA 색상각 0~360과 native 0~100을 변환하고 기기별 범위를 검사합니다. 같은 색상·색온도 숫자라도 모드 전환 명령은 생략하지 않고 실제 상태로 확인합니다.
- 기존 Web 전원·슬라이더 경로, 엔티티 ID, 방/재실·로그인·Home Monitor·Scene·TTS, 의존성과 protocol 5는 유지합니다. Bridge 앱과 HACS 통합을 모두 1.8.17로 업데이트하세요. 검증은 합성 회귀 데이터 기준이며 사용자 Hue/IKEA 실기기의 제어 성공을 직접 확인한 것은 아닙니다.

## 1.8.16

- 재실 센서와 모션 아이콘 분류를 구분합니다. 실제 카운터 상태/명령이 확인되면 기본 모델 표시를 `재실 센서 (인원 카운터)`로 보정하고 실제 하드웨어 모델은 보존합니다. Advanced 컴포넌트의 공개 센서 카테고리를 안전한 선택적 메타데이터로 전달합니다.
- 실제 `occupancy` 또는 `peopleCounter` 상태가 있는 컴포넌트에 읽기 전용 `binary_sensor` / `occupancy`를 추가합니다. 직접 재실 상태를 우선하고, 없으면 현재 인원수 0은 미재실·양의 정수는 재실로 표시합니다. 설정한 number 값, Freeze, 방향, 모션, 모바일 재택 상태로 재실을 추측하지 않습니다. 누락/잘못된 값은 미재실로 바꾸지 않으며 원본 count 및 기존 number/select/엔티티 ID를 유지합니다.
- 방 동기화의 기본값을 켬으로 변경합니다. 기존 옵션이 없으면 현재 세션에서 확인된 Advanced 기기 roomId와 같은 location의 방 목록을 사용해 잘못된 기기 영역도 교정합니다. 저장된 명시적 `sync_rooms: false`는 유지합니다. 동기화가 켜져 있으면 수동 기기 영역보다 SmartThings 방을 우선하므로 HA 기기 영역을 따로 관리하려면 끄세요. 엔티티별 수동 영역과 다른 통합 공유 기기는 변경하지 않습니다.
- HA 영역 생성/수정/삭제와 소속 기기의 영역 변경도 다시 검사합니다. 불변 메타데이터 캐시 때문에 재검사가 생략되던 부분을 보완하고, 이벤트를 병합하며 언로드 시 예약 콜백·구독을 해제합니다. 출처 없는 캐시, 다른 location, 모호한 이름으로 이동/해제하지 않습니다.
- Bridge 앱·HA 통합·패키지를 1.8.16으로 동기화합니다. 로그인·Home Monitor·Scene·speak·명령 전송·의존성과 protocol 5는 유지합니다. 자동 테스트는 합성 상태/프로필/레지스트리 기준이며 사용자 HA 설치와 실제 기기 재실/방 확인은 별도입니다.

## 1.8.15

- Advanced에서 확인한 기기 roomId·방 이름이 늦게 도착한 Web 카드/방 응답으로 되돌아가지 않도록 우선순위를 고정합니다. 누락·잘못된 roomId는 삭제로 처리하지 않고 명시적 null만 방 해제로 인정합니다.
- **기존 영역 보정:** 통합 옵션에 `SmartThings 방과 기기 영역 동기화`를 추가합니다(기본 끔). 켜면 현재 세션에서 Advanced 기기-방 연결과 같은 location의 방 목록이 모두 확인된 경우 기존 기기 영역도 동기화합니다. 출처 없는 캐시·다른 location·모호한 영역명은 덮어쓰지 않습니다. 명시적 방 해제는 영역도 해제합니다. 엔티티별 사용자 영역은 보존합니다.
- 기존 버전에는 자동 지정/사용자 지정 영역을 구분할 기록이 없으므로 업데이트만으로 모든 영역을 강제 변경하지 않습니다. 이미 잘못 지정된 영역을 고치려면 위 옵션을 켜세요. 기본 모드의 기존 영역 보존과 미지정 기기 보정은 유지합니다.
- 카드의 모델명은 실제 보고된 model 정보를 우선합니다. 그것이 없고 `peopleCounter` 정수 상태 또는 `setPeopleCounter` 정수 명령 계약이 확인되면 아이콘의 모션 분류보다 `인원 카운터` 표시를 우선합니다. 표시용 분류와 HA 엔티티 device_class는 별개이며, 기기 이름만으로 유형을 추측하지 않습니다.
- 현재 인원수는 기존 숫자형 sensor/measurement, 설정 명령은 기존 number/select로 유지합니다. 기존 unique_id·entity_id·명령 확인 방식·로그인·Home Monitor·Scene·speak·의존성과 protocol 5는 변경하지 않습니다. 새 `roomSource`는 선택적 메타데이터이며 재시작/세션 복구 후 다시 확인합니다.
- Bridge 앱과 HA 통합을 1.8.15로 함께 업데이트합니다. 회귀 검사는 합성 데이터 기반이며 사용자의 현재 SmartThings room·실행 중 HA 영역 및 네 엔티티 속성을 직접 조회·수정한 것은 아닙니다.

## 1.8.14

- 실제 Advanced 명령 카탈로그를 기반으로, Web 제어가 없는 필수 단일 enum 명령은 선택 엔티티로, 명시적 정수 범위가 있는 명령은 숫자 입력으로 보완합니다. `setUpdown`, `setFreeze`, `setPeopleCounter`의 제공된 계약을 회귀 테스트로 검증합니다. `refresh`·`push` 등 안전한 무인수 접수형 명령은 버튼으로 노출합니다.
- 사용자 정의 명령을 `switch` 상태로 추측해 거부하던 브리지 경로를 수정합니다. 새 명령 전용 엔티티는 `confirm: false`를 명시하여 정확한 Advanced 대상에 한 번 전송하고 접수 결과만 반환합니다. 실제 동작 완료로 표시하거나 입력값을 현재 상태로 저장하지 않습니다. 확인 가능한 상태 매핑이 없는 기존 `confirm: true` 요청을 몰래 접수형으로 낮추지 않습니다.
- 현재 카탈로그의 enum·정수 범위·민감 인수·중복·누락을 검사하고, 명령이 사라지거나 기기가 오프라인이면 제어를 차단합니다. 기존 Web 제어와 refresh 버튼을 우선하여 중복 생성하지 않으며 기존 엔티티 ID를 변경하지 않습니다.
- Bridge 앱·HA 통합·런타임·패키지를 1.8.14로 동기화합니다. Home Monitor 직접 제어, Scene, Galaxy Home Mini speak, 로그인 프로필, 영역 연결, 의존성 및 wire protocol 5는 유지합니다.
- 이번 검증은 제공된 안전한 카탈로그와 자동 회귀 테스트 기준입니다. 사용자 Samsung 계정에서 실제 카운터 증감·Freeze·물리 버튼 효과를 확인했다는 의미는 아닙니다. 명령 전용 숫자/선택 엔티티의 현재값은 상태 매핑 근거가 없어 `unknown`이며 실제 상태는 기존 센서로 확인합니다.

## 1.8.13

- 사용자 요청에 따라 Bridge 앱과 HA 통합을 1.8.13으로 함께 발행합니다. 1.8.12의 Home Monitor 직접 제어 동작은 변경하지 않습니다.
- 로그인된 웹앱의 위치 보안 변경 요청, 외출↔실내 전환 시 중간 해제·DOM 클릭 없음, 실제 상태 확인 및 불명확한 응답의 자동 재전송 차단을 유지합니다.
- 패키지·런타임·통합·앱의 버전 정보와 회귀 검사의 기대 버전을 동기화하고, 변경 이력을 최신 버전 순으로 정리합니다. 문서에서 1.8.11의 이전 버튼 방식과 현재 직접 요청 방식을 구분합니다.
- Scene, Advanced 기기 명령, Galaxy Home Mini speak, 로그인 프로필, 영역, 엔티티 ID, 의존성 및 wire protocol 5는 변경하지 않습니다.
- 이번 발행은 버전·배포 메타데이터와 문서 정리입니다. 새로운 실환경 동작 수정이나 삼성 계정의 전환 성공률·지연 검증을 완료했다는 의미가 아닙니다.

## 1.8.12

- Home Monitor를 화면 버튼 대신 기존 로그인된 웹앱의 `api/location.patch`와 검증된 `patchType: armStateChange` 요청으로 제어합니다. Advanced 일반 기기 명령이나 추측한 보안 URL을 사용하지 않습니다.
- 외출↔실내 전환은 목표 `ARMED_AWAY` / `ARMED_STAY`를 한 번 전송하며 중간 해제나 DOM 클릭을 하지 않습니다. 요청 접수 이후 실제 위치 보안 이벤트·새 상태 조회가 확인돼야 완료합니다.
- 클라이언트가 준비되기 전에는 제한된 정상 초기화만 기다리고, 응답이 불명확하거나 권한이 거절되면 DOM fallback·자동 재전송·자동 해제를 하지 않습니다. 미완료 요청은 같은 context에서 중복 전송을 차단합니다.
- `home_monitor_direct` 단계와 실행 버전 로그를 추가했습니다. Scene, Advanced 장치 command, Galaxy Home Mini speak, 로그인 프로필, 영역, 엔티티 ID 및 protocol 5는 유지합니다.
- 공개 SmartThings Web 2.57.0 요청 계약과 합성 Chromium/실제 명령 처리기 연결 검사를 기반으로 합니다. 사용자 삼성 계정에서의 모드 전환 승인·성공률·실제 지연은 별도 확인 대상입니다.

## 1.8.11

- 사용자 제공 실제 `section.homecard.security` / `.status-container .actions button` 구조로 Home Monitor를 제어합니다. `data-armstate`와 실제 버튼 의미를 검증하고, 문구·방패 로고·위치 Home 메뉴는 클릭하지 않습니다. 경비 중 Disarm 버튼 하나도 즉시 처리해 기존 5초 반복 탐색을 제거합니다.
- 외출↔실내 전환 요청은 같은 위치·같은 페이지·하나의 잠금 안에서 Disarm → 새 해제 상태 확인 → 요청 경비 버튼 → 최종 상태 확인 순서로 실행합니다. 중간 해제는 성공 응답이 아니며, 각 단계는 최대 한 번 클릭합니다.
- 중요: 모드 간 전환에는 짧은 경비 해제 구간이 있으며, 다음 단계가 실패하면 해제 상태에 남을 수 있습니다. 단계별 오류와 실제 상태를 전달하고 임의 재전송·자동 복원·낙관적 상태 변경은 하지 않습니다. 알려지지 않은 상태/레이아웃에서는 자동 해제하지 않습니다.
- 중간 해제 전에 수신된 최종 모드 증거는 폐기하고, 해제 확인 후 새로 구독합니다. 두 단계는 기존 명령 확인 제한을 공유하며, 다른 위치·오래된 캐시·해제 확인 실패로 다음 단계를 진행하지 않습니다.
- 실제 버튼의 보임·활성·가림·중복을 검사합니다. 포인터를 보내지 않는 trial 검사만 재확인할 수 있고, 실제 클릭의 결과가 불확실하면 같은 명령을 다시 클릭하지 않습니다.
- 기존 Scene/Advanced commands/speak, 로그인 프로필, 영역 연결, 개별 스위치, 엔티티 ID 및 protocol 5는 유지합니다. 새 네이티브 카드 회귀와 전체 CI는 합성 검증이며 실제 Samsung 계정의 성공률·지연은 별도 확인 대상입니다.

## 1.8.10

- Home Monitor 카드에서 검증한 선택기와 aria-controls/aria-owns/aria-labelledby로 직접 연결된 팝업을 같은 요청 안에서 추적합니다. 제목 없이 현재 모드를 제외한 두 선택지만 표시하는 dialog/listbox/menu도 이 연결이 확인된 경우에만 처리합니다. 연결되지 않은 팝업, 중복 대상, 비활성 제어는 계속 차단합니다.
- Home, Arm home, Armed (Home), 집 표기를 해당 Home Monitor 카드와 검증된 팝업 내부에서만 Stay로 해석합니다. 페이지 전체의 Home 메뉴나 방 이름을 누르는 규칙은 추가하지 않습니다. 실내 전환을 위해 자동으로 먼저 해제하지 않습니다.
- 기본 30초 확인 창의 7초~25초 조회 공백을 보완합니다. 상태 재확인이 활성화된 기본 경로의 조회 시작 목표는 1/2/4/7/10/15/20/25초이며 최대 8개 순차 사이클입니다. 실제 읽기 시간이 길면 늦어질 수 있고 정상 이벤트 수신 즉시 중단합니다. 제어 재전송이나 유휴 폴링은 추가하지 않습니다.
- HA가 완료 응답 이상의 시퀀스와 일치하는 실제 보안 상태를 이미 받았다면 중복 전체 인벤토리 조회를 생략합니다. 아직 뒤처졌다면 기존 1회 조회를 유지합니다. 요청값을 낙관적으로 표시하지 않습니다.
- 기존 외출/해제 직접 클릭, 확인까지 탭 유지, Scene/Advanced commands/speak, 로그인 프로필, 영역 및 개별 스위치, 엔티티 ID는 보존합니다. wire protocol은 5입니다.
- 사용자 오류 로그를 먼저 검토했으나 해당 계정의 최신 Web DOM과 단계별 시간은 확보하지 못했습니다. 선택기 패턴과 기본 조회 공백은 소스/합성 회귀로 검증하며 모든 실계정 모드 성공이나 실제 응답 시간 개선을 보장하지 않습니다.

## 1.8.9

- Home Monitor가 캐시된 모드와 같다는 이유만으로 명령을 생략하지 않습니다. 해당 위치의 새 상태 조회로 확인된 경우에만 이미 적용된 상태로 처리합니다.
- Push 누락 시 명령 대기 중에만 최대 5회의 순차적 상태 확인 사이클을 수행합니다. 날짜 없는 반대 상태의 확인은 사이클당 두 번의 읽기를 사용합니다. 정상 이벤트 수신 시 즉시 중단하고, 동시 조회·무제한 재시도·제어 명령 재전송은 하지 않습니다. 확인 제한을 15초로 설정한 예에서는 조회 시작 목표가 1·2·4·7·10초입니다. 변경하지 않은 기본 설정은 30초이며 이 경우 마지막 확인 목표는 25초입니다. 실제 조회시간에 따라 시작이 늦어질 수 있습니다.
- 날짜 없는 반대 상태는 같은 위치에서 새로 수행한 두 번의 조회가 일치하고 조회 도중 더 최신 상태가 들어오지 않은 경우에만 반영합니다. 서버 시각을 만들어 넣지 않고, 일반 스냅샷의 오래된 상태·다른 위치·중간 상태 차단을 유지합니다.
- 명령 완료 후 HA가 실제 Bridge 인벤토리를 한 번 즉시 읽어 SSE 갱신을 기다리던 구간을 보완합니다. 요청 모드를 낙관적으로 표시하지 않으며, 늦게 도착한 이전 시퀀스는 거부합니다.
- 기존 Home Monitor 직접 버튼·선택기·탭 유지, Scene·Advanced commands·speak, 방 연결·개별 스위치·로그인 프로필·엔티티 ID는 보존합니다. protocol 5를 유지합니다.
- 새 회귀 검사에는 누락 Push, 캐시 기반 잘못된 no-op, 다른 위치의 증거, 진행 중 조회 경합, 취소·상한, HA의 완료 후 상태 반영이 포함됩니다. 합성 검사와 실제 Samsung 계정의 성공률·서버 지연은 별개입니다.

## Bridge 1.8.7 후속 배포 — 2026-09-06

실제 브리지 코드 최적화, 회귀 테스트 18개 및 앱/런타임 버전 정합성을 반영했습니다. 기존 통합 v1.8.7 태그·파일·엔티티는 보존합니다. 상세 내용과 검증 한계는 [브리지 변경 문서](docs/BRIDGE_1.8.7.md)를 참조하세요.

# Changelog

## 1.8.8

- Home Monitor의 현재 모드 선택기를 해당 카드 안에서만 찾고 같은 버튼의 중복 문구를 병합합니다. 서로 다른 버튼, 비활성 제어, 다른 팝업은 계속 차단합니다.
- 선택기가 확인되면 숨겨진 직접 모드 버튼을 기다리던 반복 탐색을 건너뜁니다. 기존 직접 버튼 클릭, 실제 보안 상태 확인, 제어 탭 유지 및 대기열 제한은 유지합니다.
- 기존 HA 영역을 Unicode·공백 정규화로 재사용하고, 방 정보가 늦게 도착한 미지정 기기에 영역을 연결합니다. 사용자 지정 영역·엔티티 식별자는 덮어쓰지 않습니다.
- 일부 Web 기기 응답에서 roomId가 생략돼도 기존 방 정보를 보존합니다. 명시적 null 또는 다른 위치로 이동한 경우에는 이전 방 연결을 유지하지 않습니다.
- controlId가 지정된 개별 스위치 요청을 여러 채널의 일괄 동작으로 확대하지 않습니다. 기존 명시적 일괄 요청, 제어 허용 검사와 상태 확인은 유지합니다.
- 로컬 및 CI 회귀 검증과 실제 Samsung 계정/HA 기기 검증은 구분합니다. 실제 제어 성공률·서버 지연 개선은 사용 환경에서 별도 확인이 필요합니다.


## 1.8.7 — 2026-09-06

- 지연 알림 큐의 마지막 구독 해제 시 콜백과 빈 타이머 정리
- 즉시 전달한 버튼 등 이벤트 콜백이 이전 지연 배치에서 다시 실행되지 않도록 처리
- 같은 콜백이 다른 범위에 계속 구독된 경우 그 구독은 유지
- Advanced·실시간 SSE·장치 명령·쿠키/로그인·브리지 앱·엔티티 식별자 정책은 변경하지 않음

상세 변경·검증 범위·롤백: [docs/OPTIMIZATION_2026-09-06.md](docs/OPTIMIZATION_2026-09-06.md)
