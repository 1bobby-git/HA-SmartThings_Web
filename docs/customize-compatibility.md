# smartthings_customize Compatibility

Baseline repository: `oukene/smartthings_customize` at `6373c7ed22d6d2f46bb551e9e31955893264554f`.

No license file was detected during baseline capture, so this project treats it as behavioral reference only. No source code is copied.

Runtime YAML import is deferred until after SmartThings Web feasibility is proven.

Known public configuration surface to preserve later: `globals`, `devices`, `ignore_devices`, `ignore_platforms`, `ignore_capabilities`, `subscribe_capabilities`, `default_entity_id_format`, `entity_id_format`, `parent_entity_id`, state mappings, mode mappings, action mappings, command argument mappings, and platform inventories for sensor, binary_sensor, switch, light, lock, cover, climate, fan, number, select, button, text, vacuum, valve, update, event, and scene.

License boundary: no source-porting from `smartthings_customize` is allowed unless a compatible license is later verified. Current use is limited to public behavior and configuration compatibility notes.
