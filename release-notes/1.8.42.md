- Home Assistant의 `Number of people` / `numberOfPeople` 카운터를 사용자 조작 범위 `0~100`, 증감 단위 `1`로 제한해 65535 범위에서 슬라이더 값이 크게 튀던 문제를 개선했습니다.
- 실제 SmartThings에서 관측된 값은 임의로 잘라내지 않고 그대로 유지합니다. 제한되는 것은 Home Assistant에서 사용자가 선택하는 카운터 조작 범위뿐입니다.
- 감지 주기처럼 100을 초과하는 것이 정상인 다른 `number` 엔티티에는 원래 범위와 step을 그대로 유지하도록 회귀 테스트를 추가했습니다.
- 이번 릴리스는 Home Assistant 통합 1.8.42 업데이트입니다. Bridge 런타임/애드온은 변경되지 않아 기존 1.8.41을 그대로 사용할 수 있으며 protocol 5 호환성을 유지합니다.
